//
// bheap_gen.h
// Generated by AMCC. Do not edit.
//
// What each function below is guaranteed to do is stated in Lean and
// machine-checked against this generator, for every accepted schema.
// See docs/GOALS.md.
//

#pragma once
#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

typedef struct timer_row {
  uint64_t id;
  uint32_t bh_active_idx;
  bool bh_active_inheap;
} timer_row;

typedef struct TimerDb {
  struct timer_row *bh_active_elems[1024];
  uint32_t bh_active_n;
} TimerDb;

extern struct TimerDb g_TimerDb;

// func:TimerDb_bh_active_Init
void TimerDb_bh_active_Init(void);

// func:TimerDb_bh_active_N
uint32_t TimerDb_bh_active_N(void);

// func:TimerDb_bh_active_EmptyQ
bool TimerDb_bh_active_EmptyQ(void);

// func:TimerDb_bh_active_InBheapQ
bool TimerDb_bh_active_InBheapQ(struct timer_row *row);

// func:TimerDb_bh_active_First
struct timer_row *TimerDb_bh_active_First(void);
